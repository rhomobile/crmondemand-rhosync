<div class="pageTitle">
  <h1>Activities</h1>
</div>

<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="<%= Rho::RhoConfig.start_path %>">Home</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :new %>">New</a>
  </div>
</div>

<div class="content">
  <ul>
    <% @activities.each do |activity| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => activity.object %>">
            <span class="title"><%= activity.Subject %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>