<div class="pageTitle">
  <h1>Leads</h1>
</div>

<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="<%= Rho::RhoConfig.start_path %>">Home</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :new %>">New</a>
  </div>
</div>

<div class="content">
  <ul>
    <% @leads.each do |lead| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => lead.object %>">
            <span class="title"><%= lead.LeadFirstName%> <%=lead.LeadLastName %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>