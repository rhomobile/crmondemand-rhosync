<div id="pageTitle">
		<h1>Edit <%= @lead.Lead_id%></h1>
</div>

<div id="toolbar">
    <%= link_to "Back", :action => :show, :id => @lead.object %>
    <%= link_to "Delete", :action => :delete, :id => @lead.object %>
</div>


<div id="content">
    <form method="POST" action="<%= url_for :action => :update %>">
    	<input type="hidden" name="id" value="<%= @lead.object %>"/>
            <table>
        		
                    <tr>
            			<td class="itemLabel">Lead: </td>
              			<td class="itemValue"><input type="text" name="lead[Lead_id]" value="<%= @lead.Lead_id %>"/></td>
          		    </tr>
        		
            </table>
            <input type="submit" class="standardButton" value="Update" />
    </form>
</div>
