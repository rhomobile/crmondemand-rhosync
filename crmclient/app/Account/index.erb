<div class="pageTitle">
  <h1>Accounts</h1>
</div>

<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="<%= Rho::RhoConfig.start_path %>">Home</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :new %>">New</a>
  </div>
</div>

<div class="content">
  <ul>
    <% @accounts.each do |account| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => account.object %>">
            <span class="title"><%= account.AccountName %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>